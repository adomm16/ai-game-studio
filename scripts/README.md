# Doğrulama

Çalıştırma: `python scripts/validate_studio.py`. Yalnızca Python standard library kullanır. Başarısız kontrolde anlaşılır hata ve non-zero exit code üretir. Test: `python -m unittest discover -s tests -v`.
