# DEC-STUDIO-NAME-001 Market Source Register

The canonical CSV contains 400 Source IDs. Added fields record source-origin group, independence classification/pair, activity and primary qualification, accessibility, claim review, final URL, and redirect chain.

The designated row reviewer is repository Agent ID `quality-assurance-director`, distinct from collector `market-research-director`. The live network pass timed out at the environment gateway. Consequently all 400 rows are `NEEDS_RECHECK` / `RECHECK_REQUIRED`; none qualifies as activity evidence. No broken, blocked, or recheck-required source is promoted to qualifying evidence.

The separate source-review ledger covers every Source ID and preserves the fail-closed result. A later independent reviewer must reopen each URL, verify identity and claim support, record redirects, and only then assign `REVIEWED`.
